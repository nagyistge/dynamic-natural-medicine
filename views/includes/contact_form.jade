form#contact-form.row(method="post", action="")
  .row
    .medium-6.columns
      input(type="text", name="name", placeholder="What should we call you?", required)
    .medium-6.columns
      input(type="email", name="email", placeholder="Where can we email you back?", required)
  .row
    .small-12.columns
      textarea(name="message", placeholder="How can we help you?")
      button.g.submit(type="button", data-event="contact.send") Send It
    // TODO: if success, replace form part, other wise show erro
// TODO: error handling
//  .grid.pad
    .form-plant
      //p.plant
        | Your message has been delivered.
        br
        | We will get back to you in a jiff.
      .g-1-2.boxxy.small-6

      .g-1-2.boxxy.small-6

      .g-1.boxxy.small-12
